import { Form } from 'formik';
import styled from 'styled-components'

export const StyleFormHome = styled(Form)`
  display: flex;
  flex-direction: column;
  gap: 1rem;
  padding: 1rem;
  width: 500px;
  height: auto;
`