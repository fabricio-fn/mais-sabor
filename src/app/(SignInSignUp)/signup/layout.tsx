import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sign Up | + Flavor',
  description: 'Join our community. Create an account quickly and enjoy exclusive benefits. Register now!"',
}

export default function SignUpLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return ( children )
}